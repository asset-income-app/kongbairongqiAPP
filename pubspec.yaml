name: blankos
description: "BlankOS - 零抽成插件化容器应用"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  webview_flutter: ^4.4.2
  path_provider: ^2.1.1
  archive: ^3.4.9
  shared_preferences: ^2.2.2
  file_picker: ^6.1.1
  http: ^1.2.0
  flutter_local_notifications: ^17.0.0
  package_info_plus: ^5.0.1
  crypto: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
